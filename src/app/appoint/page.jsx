export const metadata = {
  title: "Docappoint-Appointment",
  description: "A doctor appointment website",
};

import AppointmentClient from "../../components/AppointmentClient";

export default function Page() {
  return <AppointmentClient />;
}